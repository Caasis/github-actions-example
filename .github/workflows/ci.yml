name: Build
on:
  push:
    branches: [master, main]
  pull_request:
    types: [opened, synchronize]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: docker/setup-buildx-action@v3
      - name: Build image
        run: docker buildx build . -o type=oci,dest=image.tar
      - name: Push image to Caasis
        run: gzip -c image.tar | curl -Lu "$CAASIS_API_KEY" --data-binary @- https://api.caasis.cloud/new > ~/url
        if: ${{ github.head_ref }}
        env:
          CAASIS_API_KEY: ${{ secrets.CAASIS_API_KEY }}
      - name: Comment on Pull Request
        run: 'echo "Preview environment: $(cat ~/url)" | gh pr comment --create-if-none --edit-last ${{ github.head_ref }} -F-'
        if: ${{ github.head_ref }}
        env:
          GH_TOKEN: ${{ github.token }}
